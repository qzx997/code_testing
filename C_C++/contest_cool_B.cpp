#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
const ll maxn = 1e7 + 7;
const ll mod = 998244353;
#define pi atan(1.0)*4

struct Node{
	int a, l, r, k;
} node[maxn];

int main()
{
	ios::sync_with_stdio(false);
	
	ll n, q;
	cin >> n >> k;
	int opt;
	cin >> opt;
	if(opt == 1) 
	{
		int a, l, r, k;
		cin >> a >> l >> r >> k;
		
	}
	
	
	return 0;
}
